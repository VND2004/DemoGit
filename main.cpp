#include <bits/stdc++.h>

using namespace std;
float n;
string s;
int main()
{
    cin >> n;
    s = (n >= 8) ? "gioi" : (n >= 6.5 ? "kha" : (n >= 5 ? "Trung binh" : "yeu"));
    cout << s;
    cout << "Test_thu_git"
}
